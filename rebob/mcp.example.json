{
  "_comment": "Prefer: pip install rebob && rebob init (writes .bob/mcp.json automatically).",
  "mcpServers": {
    "rebob": {
      "command": "<venv>/bin/python",
      "args": ["-m", "rebob.server"],
      "cwd": "<project-root>"
    }
  }
}
